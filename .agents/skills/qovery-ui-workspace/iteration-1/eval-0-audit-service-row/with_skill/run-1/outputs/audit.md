# /qovery-ui audit — `audit-input.tsx`

| Location                    | Severity      | Issue                                                                                                                         | Fix                                                                                                                              |
| --------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `audit-input.tsx:2`         | `[blocking]`  | Imports deprecated `Tag` and internal `ButtonPrimitive` from `@qovery/shared/ui`.                                             | Import `Badge` and `Button` instead; remove `Tag` and `ButtonPrimitive`.                                                         |
| `audit-input.tsx:14`        | `[blocking]`  | `text-green-500` (raw Tailwind palette) used for the `running` status — hardcoded, non-semantic.                              | Use the semantic token `text-positive`, or replace the whole custom status pattern with `<StatusChip status="RUNNING" />`.       |
| `audit-input.tsx:16`        | `[blocking]`  | `text-yellow-500` used for `degraded` status — raw Tailwind palette, not semantic.                                            | Use `text-warning`, or prefer `<StatusChip>` for system status.                                                                   |
| `audit-input.tsx:17`        | `[blocking]`  | `text-red-500` used for `stopped` status — raw Tailwind palette, not semantic.                                                | Use `text-negative`, or prefer `<StatusChip>`.                                                                                    |
| `audit-input.tsx:12-23`     | `[blocking]`  | Custom colored status indicator (text + color only) bypasses the single source of truth for status.                           | Replace the bespoke `statusColor` ternary with `<StatusChip status={...} />`, which bundles icon + color + tooltip.              |
| `audit-input.tsx:23`        | `[blocking]`  | Color-only status state — accessibility failure (no icon, no tooltip, color alone communicates `running`/`degraded`/`stopped`). | Use `<StatusChip>` (icon + label + tooltip) or pair the color with an icon and `aria-label`.                                     |
| `audit-input.tsx:27`        | `[blocking]`  | `<Tag>` is deprecated.                                                                                                        | Replace with `<Badge color="green">v1.2.3</Badge>` — but see next row about decorative status color.                              |
| `audit-input.tsx:27`        | `[blocking]`  | Status color (`green`) used decoratively for a version label — `green` is reserved for `positive` system state.               | Use a neutral or brand-colored `<Badge>` for the version tag (e.g. `<Badge color="neutral">v1.2.3</Badge>`).                      |
| `audit-input.tsx:29`        | `[blocking]`  | `<ButtonPrimitive>` used directly in product code (it is the internal primitive).                                             | Replace with `<Button variant="solid" color="brand" onClick={onRestart}>Restart</Button>`.                                       |
| `audit-input.tsx:31`        | `[blocking]`  | `focus:outline-none` suppresses the focus ring — accessibility non-negotiable.                                                | Remove the override; let `<Button>` provide the built-in `focus-visible:outline-2` ring.                                         |
| `audit-input.tsx:32`        | `[blocking]`  | Hardcoded hex (`#7c3aed`) and hardcoded `white` via inline `style` — breaks light/dark parity and bypasses the token system.  | Drop the inline style; use `<Button color="brand" variant="solid">` which resolves to `bg-surface-brand-solid` automatically.   |
| `audit-input.tsx:37`        | `[blocking]`  | Icon-only `<button>` (delete `X`) has no `aria-label` — screen readers will announce nothing.                                 | Add `aria-label="Delete service"` (and prefer wrapping the icon in `<Button>` so focus ring and styling come for free).         |
| `audit-input.tsx:37`        | `[blocking]`  | Native `<button>` has no visible focus ring (no `focus-visible:outline-*`).                                                   | Use `<Button>` (or add `focus-visible:outline-2 focus-visible:outline-brand-strong`).                                            |
| `audit-input.tsx:38-40`     | `[important]` | Inline raw `<svg>` for a delete glyph instead of the shared icon system.                                                      | Use `<Icon iconStyle="regular" iconName="trash" />` from the FA Pro kit; do not roll a custom inline X glyph.                    |
| `audit-input.tsx:20`        | `[important]` | `gap-5` (20px) and `p-5` (20px) are off the 4px grid — arbitrary mid-range values, not the allowed 2px/6px fine-tuning case.  | Snap to grid: `gap-4` (16px) and `p-4` (16px), or `gap-3` / `p-3` (12px) for a denser row.                                       |
| `audit-input.tsx:20`        | `[important]` | Legacy neutral palette `border-neutral-300` (deprecated 50–900 scale).                                                        | Use semantic `border-neutral` (resolves to the correct token across themes).                                                     |
| `audit-input.tsx:20`        | `[nit]`       | Bare `rounded` — likely too tight for a row at this padding; concentric radius unspecified.                                   | Pick an explicit radius matching the surface: e.g. `rounded-md` or `rounded-lg`, and document the concentric relationship.       |
| `audit-input.tsx:22`        | `[important]` | Legacy neutral palette `text-neutral-700`.                                                                                    | Use semantic `text-neutral` (default body text).                                                                                 |
| `audit-input.tsx:23`        | `[important]` | `text-xs` mixed with `text-sm` on the same row introduces a third size for tiny metadata; status label is also lowercase.     | Use `text-ssm`/`text-sm` with `text-neutral-subtle`, or — preferred — replace with `<StatusChip>` which standardizes the size.   |
| `audit-input.tsx:31`        | `[important]` | Manual button sizing (`px-3 py-2 text-sm`) overrides the shared button height/typography.                                     | Remove the size overrides and rely on `<Button size="sm">` (or default) for consistent height across the row.                    |
| `audit-input.tsx:37`        | `[nit]`       | Hover state uses legacy palette `hover:bg-neutral-100`.                                                                       | Use `hover:bg-surface-neutral-componentHover` (or `hover:bg-surface-neutral-component`).                                          |
| `audit-input.tsx:23`        | `[nit]`       | Status string rendered raw (`running` / `degraded` / `stopped`) in lowercase code values.                                     | Render a human label ("Running", "Degraded", "Stopped"). `<StatusChip>` handles this mapping.                                    |

---

## Totals

- `[blocking]`: 13
- `[important]`: 6
- `[nit]`: 3
- **Total: 22 issues**
