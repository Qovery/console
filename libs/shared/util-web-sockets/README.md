# shared-util-web-sockets

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test shared-util-web-sockets` to execute the unit tests via [Jest](https://jestjs.io).
