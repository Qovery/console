import { useClickAway, useDocumentTitle } from '@uidotdev/usehooks'

// NOTE: if you export from `@uidotdev/usehooks`, you need to mock it on the `mock.ts` file for tests
export { useDocumentTitle, useClickAway }
